#include <set>
#include <vector>

std::vector<std::set<std::size_t> > mmio2_reader(const char* filename,
                                                 std::size_t& n_vert,
                                                 std::size_t& n_edges);

